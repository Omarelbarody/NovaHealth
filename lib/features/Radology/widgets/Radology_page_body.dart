import 'package:flutter/material.dart';

class RadologyBody extends StatelessWidget {
  const RadologyBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}